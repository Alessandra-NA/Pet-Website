<h1>Administración de establecimientos Pet Friendly</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nombre del establecimiento</th>
      <th scope="col">Calificación</th>
      <th scope="col">Tipo</th>
      <th scope="col"></th>
      <th scope="col"></th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <% establecimientos.forEach(function(establecimiento){ %>
    <tr>
        <th scope="row">
          <%=establecimiento.id%>
        </th>
        <td>
          <%=establecimiento.name%>
        </td>
        <td>
          <%=establecimiento.rating%>
        </td>
        <td>
          <%=establecimiento.type%>
        </td>

        <td>
            <a href="/accounts/verSugerencias/<%= establecimiento.id %>" class="btn btn-primary">Ver sugerencias de cambio</a>
        </td>

        <td>
            <button class="btn btn-primary">Ver reportes no Pet Friendly</button>
        </td>

        <td>
            <button data-bs-toggle="modal" data-bs-target="#deleteModal" class="btn btn-danger deleteButton"
            estid="<%=establecimiento.id%>" username="<%=establecimiento.name%>">Borrar</button>
        </td>
    </tr>
      <% }) %>
  </tbody>
</table>

<div class="modal" id="deleteModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Borrar usuario</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body" id="deleteMessage">
        <p>Modal body text goes here.</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" usertoDelete="" class="btn btn-danger" userid="" id="confirmDeleteButton">Borrar</button>
      </div>
    </div>
  </div>
</div>

<div class="modal" id="detailsModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Borrar usuario</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body" id="deleteMessage">
        <p>Modal body text goes here.</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" usertoDelete="" class="btn btn-danger" userid="" id="confirmDeleteButton">Borrar</button>
      </div>
    </div>
  </div>
</div>


<script>
  highlightNavbar(4)
  document.querySelectorAll('.deleteButton').forEach((button) => {
    button.addEventListener('click', (ev) => {
      console.log(ev.target);
      document.querySelector('#deleteMessage').innerText = "  Está seguro que desea borrar el establecimiento " + ev.target.getAttribute('username') + '?';
      document.querySelector('#confirmDeleteButton').setAttribute("estid", ev.target.getAttribute('estid'));
    })
  })
  document.querySelector('#confirmDeleteButton').addEventListener('click', (ev) => window.location = '/accounts/deleteEstablecimiento/' + ev.target.getAttribute('estid'))
</script>