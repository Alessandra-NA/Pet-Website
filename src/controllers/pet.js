const {Pet,} = require('../models')

module.exports = {
  /**
   * @param {import('express').Request} req
   * @param {import('express').Response} res
  */

  /* getPetPost: async (req, res) => {
    try{}
    catch{}
  } */
}
